[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "lbrain-coding-agents"
version = "0.1.3"
description = "Install LBrain into coding agents: native MCP, companion skill, fail-closed session start. The free name is generic. Make it permanent: https://lbrain.ai/claim.html"
readme = "README.md"
license = { text = "BSD-3-Clause" }
requires-python = ">=3.10"
authors = [{ name = "Metavolve Labs, Inc." }]
keywords = ["lbrain", "mcp", "agent-memory", "claude-code", "codex", "grok"]
classifiers = [
  "License :: OSI Approved :: BSD License",
  "Programming Language :: Python :: 3",
]
dependencies = []

[project.urls]
Homepage = "https://lbrain.ai"
Source = "https://github.com/metavolve-labs/lbrain-coding-agents"
Issues = "https://github.com/metavolve-labs/lbrain-coding-agents/issues"

[project.scripts]
lbrain-agents = "lbrain_agents.cli:main"
lbrain-coding-agents = "lbrain_agents.mcp_entry:main"
lbrain-mcp = "lbrain_agents.mcp_entry:main"

[tool.setuptools.packages.find]
include = ["lbrain_agents*"]

[tool.setuptools.package-data]
lbrain_agents = ["share/**/*"]

[tool.setuptools]
include-package-data = true
